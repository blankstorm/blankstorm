precision highp float;
#include "utils.glslx"

varying vec2 vUV;

uniform int face;
uniform sampler2D sampler;
uniform float resolution;
uniform float seed;

export void fragmentSource() {
	float color = scalar_field(face, vUV, seed, resolution, sampler);
	gl_FragColor = vec4(vec3(color), 1.0);
}