precision highp float;
#include "utils.glslx"

varying vec2 vUV;

uniform int face;
uniform float resolution;
uniform float base;
uniform float seed;

export void fragmentSource() {
	float color = scalar_field(face, vUV, base, resolution, seed);
	gl_FragColor = vec4(vec3(color), 1.0);
}